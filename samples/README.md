# Sample files

Openable demos (drop on the app — they load showing the logo as cover):

- `sample.pkg` — minimal CNT metadata + SFO + logo cover
- `sample.exfat` — minimal exFAT image (sce_sys/param.json + logo cover)
- `sample.ffpfsc` — compressed PFS wrapping the same exFAT image

Dummy file (not real data), only for seeing the file icon after installing:

- `sample.ffpkg` — UFS2 images can't be synthesized; needs a real dump
