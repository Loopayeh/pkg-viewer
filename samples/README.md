# Sample files

- `sample.pkg` — minimal openable demo (CNT metadata + SFO + logo cover).
  Drop it on the app and it loads like a real game, showing the logo as cover.
- `sample.exfat` / `sample.ffpfsc` / `sample.ffpkg` — dummy files
  (not real game data), just so you can see the file icons after installing.
